@model IEnumerable<WebBookStoreManage.Models.DANHMUCCHITIET>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenDanhMucCT)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DanhMuc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenDanhMucCT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DanhMuc.TenDanhMuc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDanhMucCT">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDanhMucCT">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDanhMucCT">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
