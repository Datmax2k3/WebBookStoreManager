@model IEnumerable<WebBookStoreManage.Models.HINHANHSANPHAM>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UrlAnh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SanPham)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UrlAnh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SanPham.IdSanPham)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdHinhAnh">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdHinhAnh">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdHinhAnh">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
