@model IEnumerable<WebBookStoreManage.Models.DANHGIA>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NoiDung)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SanPham)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NguoiDung)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NoiDung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SanPham.IdSanPham)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NguoiDung.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDanhGia">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDanhGia">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDanhGia">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
